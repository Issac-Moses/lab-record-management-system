# ───────────────────────────────────────────────
# Node / npm
# ───────────────────────────────────────────────
node_modules
dist
.npm

# ───────────────────────────────────────────────
# Git & GitHub
# ───────────────────────────────────────────────
.git
.github
.gitignore

# ───────────────────────────────────────────────
# Environment — NEVER bake secrets into images
# ───────────────────────────────────────────────
.env
.env.*
!.env.example

# ───────────────────────────────────────────────
# IDE / editor noise
# ───────────────────────────────────────────────
.vscode
.cursor
.cursor-tutor
*.log
coverage

# ───────────────────────────────────────────────
# Large generated build artefact — not needed in any image
# ───────────────────────────────────────────────
# NOTE: src/ and public/ are intentionally NOT excluded here.
#   • Dockerfile.frontend needs them for "vite build" (COPY src/ and COPY public/).
#   • Dockerfile (backend) uses only explicit COPY server.cjs / routes/ / middleware/ /
#     services/ — so src/ and public/ are in the build context but never copied in.
# ───────────────────────────────────────────────
stats.html

# ───────────────────────────────────────────────
# Test / QA artefacts
# ───────────────────────────────────────────────
tests
playwright.config.ts
vitest.config.ts
playwright-report
test-results

# ───────────────────────────────────────────────
# Documentation & markdown
# ───────────────────────────────────────────────
docs
*.md
!README.md

# ───────────────────────────────────────────────
# SQL migrations & CSV data files
# ───────────────────────────────────────────────
*.sql
*.csv

# ───────────────────────────────────────────────
# Shell scripts (not needed inside container)
# ───────────────────────────────────────────────
*.sh

# ───────────────────────────────────────────────
# Runtime job directories (use Docker volume instead)
# ───────────────────────────────────────────────
jobs
tmp
.tmp-*

# ───────────────────────────────────────────────
# Python / proctor service
# ───────────────────────────────────────────────
backend/proctor
__pycache__
*.pyc
.venv
venv
